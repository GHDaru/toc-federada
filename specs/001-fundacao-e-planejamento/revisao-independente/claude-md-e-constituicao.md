# Veredito — claude-md-e-constituicao

> Julgamento **às cegas** por crítico independente em contexto fresco: ele não
> sabia qual documento era de quem, e foi instruído a escolher A ou B sem empate.
> Rodada: rodada 1 — corpus de planejamento contra a barra.
> Barra (documento B): CLAUDE.md + constituição da irmã.
> **Transcrição literal do que o crítico devolveu** — não editada, não resumida,
> não suavizada, inclusive onde reprova o nosso lado.

- **Escolha**: documento **A** — nosso documento

## Por que

A vence em verificabilidade, que é o critério de maior peso. Onde B afirma o defeito fundador de forma inauditável — "O protótipo que originou este projeto chamava a interface de programação (API) do provedor de modelo **direto do navegador**" (gestaodeprioridades/docs/governance/constitution.md, P7), sem repositório, arquivo ou linha —, A cita `tocbuilderv3/services/geminiService.ts:16` **com o código colado**, e eu confirmei: a linha 16 é exatamente `const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });`. O mesmo padrão se repete na referência normativa do P2: B remete a "`GHDaru/protocolos` (padrão APH) e o guia do desenvolvedor"; A dá `padrao/padrao-aph.md` e `docs/integration/guia-desenvolvedor-app-federada.md`, e os dois arquivos existem. A também converte regra em portão onde B só exorta: a regra R3 dos dois documentos exige o campo "Princípios tocados" em todo Registro de Decisão Arquitetural (ADR), mas `grep -c "Princípios tocados" /home/user/gestaodeprioridades/scripts/check-adrs.sh` devolve **0** contra **6** em `/home/user/toc-federada/scripts/check-adrs-sucessao.sh` — que rodei e passou imprimindo o tamanho do que olhou ("ADRs examinados: 8 · verificações executadas: 32", saída 0), satisfazendo a própria R2. E A preserva o bloco do método instalado inteiro, inclusive a cláusula "Never REWRITE … a `PreToolUse` guard refuses the rewrite, so this is enforced, not asked": alimentei o hook com um evento real e ele devolveu `"permissionDecision": "deny"` sobre `docs/adr/0005-…md`; B não tem essa cláusula nem `.claude/settings.json` (arquivo inexistente). B ganha em dois pontos reais — todos os seus números batem (linha 38 do `check-links.sh`, 21 capturas, 114 tarefas, cinco nomes em `responsavel`) e o `check-conformance.sh` dele passa enquanto o de A falha — mas isso não compensa um princípio inegociável (P7) ancorado em prosa e duas regras de retrospectiva sem portão.

## A maior lacuna restante

B transforma em texto o que A transforma em portão: as duas regras que B pagou caro para aprender não têm função de aptidão nenhuma. (1) R3 exige "Todo ADR declara agora um campo **Princípios tocados**, com `nenhum` escrito por extenso" — mas `grep -c "Princípios tocados" /home/user/gestaodeprioridades/scripts/check-adrs.sh` → `0`; o script roda ("ADRs examinados: 19 · relações de sucessão conferidas: 3", saída 0) e não olha para o campo, ou seja, o defeito exato do ADR 0011→0016 (decidir matéria do P2 INEGOCIÁVEL sem citá-lo) volta a passar verde. (2) A imutabilidade do ADR e do `docs/records/decisoes.jsonl` é pedida, não imposta: `/home/user/gestaodeprioridades/.claude/settings.json` não existe e `/home/user/gestaodeprioridades/scripts/hooks/` não existe. Ação concreta: (a) acrescentar ao `scripts/check-adrs.sh` um invariante por ADR que exija a linha `**Princípios tocados**:` com valor não vazio (aceitando literalmente `nenhum`) e imprima "ADRs examinados: N · verificações executadas: M", falhando com saída 1 — hoje há 19 ADRs, então o baseline é 19 verificações novas; (b) copiar `scripts/hooks/guard-immutables.py` e o `.claude/settings.json` de `/home/user/toc-federada` (matcher `Edit|Write|NotebookEdit|MultiEdit` em `PreToolUse`) e validar com o payload `{"hook_event_name":"PreToolUse","cwd":"/home/user/gestaodeprioridades","tool_name":"Edit","tool_input":{"file_path":"docs/adr/0011-….md"}}`, esperando `"permissionDecision": "deny"`; (c) só então citar os dois portões pelo nome no CLAUDE.md, ao lado de R3 e R5, como A faz.

## Defeitos factuais apontados

- /home/user/toc-federada/CLAUDE.md:135-136 e /home/user/toc-federada/docs/governance/constitution.md:199 — DEFEITO DE HONESTIDADE, o mais grave de A. Os dois textos apresentam `scripts/check-conformance.sh <NNN>` como a resposta executável a 'você está seguindo o método?'. O portão FALHA neste repositório: `bash /home/user/toc-federada/scripts/check-conformance.sh 001` → saída 1, imprimindo `✗ mutation floor 55 is above the newest cycle 012 — TAIL:mutation was charged to nobody.` e `✗ declared-absence floor 61 is above the newest cycle 012 — 'pendente' would pass as evidence everywhere.` e `✗ the method did not survive into the artifacts of at least one cycle.` Sem argumento é pior: `✗ no cycle in range (floor 42) — the gate checked nothing.` Os pisos 42/55/61 são a numeração de ciclos do `GHDaru/maestro`, incompatível com a numeração 001–012 daqui. Nenhum documento declara essa dívida: `grep -rn 'check-conformance|floor' docs/roadmap.md CHANGELOG.md docs/adr/*.md` não devolve nada. No documento B o mesmo portão passa: `bash /home/user/gestaodeprioridades/scripts/check-conformance.sh 002` → saída 0, `✓ every cycle checked declares its artifacts and carries the closing tail with evidence.`
- /home/user/toc-federada/CLAUDE.md:138 — cita `docs/ecosystem/ideias/` como um dos artefatos que nunca se reescreve. O diretório não existe: `ls /home/user/toc-federada/docs/ecosystem` → `No such file or directory`. O `check-caminhos.sh` responde verde só porque o caminho está explicitamente isentado em `/home/user/toc-federada/scripts/check-caminhos.sh:69` ('diretório do GHDaru/maestro citado pelo bloco instalado do método'). É uma isenção declarada, não uma mentira — mas é exatamente o buraco que a regra R4 do próprio documento existe para fechar, e o agente é instruído a fazer append num diretório inexistente.
- /home/user/toc-federada/CLAUDE.md:92-94 (R5) — REGRESSÃO EM RELAÇÃO A B, verificada. A afirma que `scripts/check-adrs-sucessao.sh` 'também exige o ADR no índice do `docs/adr/README.md`'; o índice do próprio repositório (`docs/adr/README.md:11`) atribui essa mesma verificação a `scripts/check-adr.sh`. Os dois scripts existem (`check-adr.sh`, 9565 bytes; `check-adrs-sucessao.sh`, 11620 bytes) e disputam a mesma função — duplicação que o Princípio VI do Maestro proíbe ('Never duplicate a function already served') e citação divergente do mesmo dever em dois lugares.
- /home/user/toc-federada/CLAUDE.md — omissão material herdada de B: A perdeu a regra 'Constitution Check has TWO tables: Maestro principles I–VIII and project principles P1–P7. A plan with only the first is incomplete', presente em /home/user/gestaodeprioridades/CLAUDE.md. Verificado: `grep -c 'TWO tables|duas tabelas' /home/user/toc-federada/CLAUDE.md` → 0. Agrava-se porque a descrição da skill listada em A (CLAUDE.md:122) diz literalmente 'Produces the Constitution Check table (Maestro Principles I–VIII)': um agente que leia só A produzirá um Constitution Check sem os P1–P7, isto é, sem checar os três princípios INEGOCIÁVEIS do próprio projeto.
- /home/user/gestaodeprioridades/CLAUDE.md, R3 — afirmação sem portão: 'Todo ADR declara agora um campo Princípios tocados, com `nenhum` escrito por extenso quando for o caso.' Nenhum script verifica: `grep -c 'Princípios tocados' /home/user/gestaodeprioridades/scripts/check-adrs.sh` → 0. Para contraste, o mesmo grep em `/home/user/toc-federada/scripts/check-adrs-sucessao.sh` → 6, e a execução confirma o invariante na saída: 'invariantes por ADR: 4 (índice · registro append-only · campo "Princípios tocados" · campo "Sucede")'.
- CONFERÊNCIAS QUE BATERAM (registrado para que o veredito não pareça seletivo). B: `check-links.sh` linha 38 é de fato a linha do `sed 's/`[^`]*`//g'` (grep -n confirma linha 38); `ls docs/jornadas/capturas | wc -l` → 21; `len(fixture['tarefas'])` → 114; nomes distintos não nulos em `responsavel` → 5 (Charles, Daru, Gesse, Marcelo Alves, Susana); `docs/produto/arquitetura-de-entrega.md` de fato não existe e `docs/integracao/` existe; '7 verdes, 17 testes e 166' aparece textualmente em specs/002*/qa-report.md:126; `scripts/check-caminhos.sh` → saída 0, 280 caminhos; `scripts/check-adrs.sh` → saída 0, 19 ADRs. A: `check-caminhos.sh` → saída 0, 523 caminhos em 73 arquivos; `check-adrs-sucessao.sh` → saída 0, 32 verificações; o hook `guard-immutables.py` devolve `deny` para edição de ADR commitado; `padrao/padrao-aph.md` e `docs/integration/guia-desenvolvedor-app-federada.md` existem; o alcance do P2 está mesmo no texto do princípio desde a versão 1.0.0 (constitution.md:64-70).

## Lacunas secundárias

- /home/user/gestaodeprioridades/docs/governance/constitution.md, P7: a violação canônica é descrita sem endereço ('O protótipo que originou este projeto chamava a interface de programação (API) do provedor de modelo direto do navegador'), o que viola a própria R1 do documento. Substituir por `tocbuilderv3/services/geminiService.ts:16` com a linha colada — verifiquei que é `const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });`.
- /home/user/gestaodeprioridades/docs/governance/constitution.md, P2, seção 'Referência normativa': cita apenas nomes de repositório. Trocar por caminhos abríveis — `GHDaru/protocolos` → `padrao/padrao-aph.md` (Anexos A e B) e `GHDaru/ghdaru` → `docs/integration/guia-desenvolvedor-app-federada.md`; ambos existem e ambos passariam pelo `scripts/check-caminhos.sh` do próprio repositório.
- /home/user/gestaodeprioridades/CLAUDE.md, bloco de aviso do topo: admite 'Não há portão que verifique isto' sobre dado real de pessoa e para aí. A honestidade é boa, a inércia não: escrever `scripts/check-base-sintetica.sh` que falhe se `prototipo/dados/fixture.json` contiver qualquer um dos cinco nomes hoje presentes em `responsavel` (Charles, Daru, Gesse, Marcelo Alves, Susana) e que conte as 21 imagens de `docs/jornadas/capturas/`, imprimindo o tamanho examinado — hoje 114 tarefas e 21 capturas.
- /home/user/gestaodeprioridades/CLAUDE.md, bloco 'Method: Maestro': está desatualizado em relação ao instalável canônico — falta o auto-import `@docs/governance/principles.md`, falta `TAIL:mutation` na lista da Cauda (A tem: `TAIL:review`, `TAIL:security`, `TAIL:mutation`, `TAIL:gate`) e falta o item 'Never REWRITE … PreToolUse guard'. Reinstalar o bloco a partir de `/home/user/maestro/CLAUDE.md` e conferir com `scripts/check-install.sh`, que já existe no repositório.
